Post = a single message
Topic = a collection of post
Forum = a collection of topics

------------------------
TO CREATE:
Forum Entity (+ Controller and Repository + Pages)
Topic Entity (+ Controller and Repository + Pages)
Post Entity (+ Controller and Repository  + pages)
User Profile ( UserProfileController and etc)

After finishing post, topic and forum, complete:
Management Control Panel (Not an Entity, Create an HTML page and controller)
Private messaging system (+ Controller and Repository + Pages)


------------------------
TO FINISH
Registration process (need to add warning messages when user exists)
  
-----------------------
TO SOLVE:
Fix all of the Authentication issue (Security)